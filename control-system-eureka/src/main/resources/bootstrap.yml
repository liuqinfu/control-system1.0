spring:
  application:
    name: eurekaserver
  cloud:
    config:
      uri: http://localhost:8081
      fail-fast: true

eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: false
    fetchRegistry: false
    server:
      waitTimeInMsWhenSyncEmpty: 0